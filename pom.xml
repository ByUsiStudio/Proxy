<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>miao.byusi.hp</groupId>
    <artifactId>proxy</artifactId>
    <packaging>pom</packaging>
    <version>16.0</version>

    <parent>
        <artifactId>hserver-parent</artifactId>
        <groupId>cn.hserver</groupId>
        <version>3.3.M2</version>
        <!-- <version>3.6.M9</version> -->
    </parent>


    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <!--
          构建输出目录后缀：默认空 → 各模块仍是 ./target（行为不变）。
          允许多个构建并行而不互相踩踏，例如：
            mvn -o -DskipTests compile -Dbuild.suffix=-b
          会把输出放到 ./target-b。多人/多任务同时改同一个工作树时很有用。
        -->
        <build.suffix></build.suffix>
    </properties>

    <modules>
        <module>proxy-common</module>
        <module>proxy-server</module>
        <module>proxy-proxy</module>
    </modules>

    <dependencies>
        <dependency>
            <groupId>cn.hserver</groupId>
            <artifactId>hserver</artifactId>
        </dependency>

        <dependency>
            <groupId>commons-cli</groupId>
            <artifactId>commons-cli</artifactId>
            <version>1.4</version>
        </dependency>
    </dependencies>

    <build>
        <!-- 见 properties 中 build.suffix 的说明：默认仍是 ./target -->
        <directory>${project.basedir}/target${build.suffix}</directory>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>enforce-jdk-25</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <fail>true</fail>
                            <rules>
                                <requireJavaVersion>
                                    <version>[25,)</version>
                                    <message>
构建需要 JDK 25 或更高版本，但当前 Maven 使用的 Java 版本低于 25。
（Maven 只读取 JAVA_HOME，不会使用 PATH 中的 java。）

请任选一种方式修复后重试：

  1) PowerShell（仅当前窗口生效，最快）：
       $env:JAVA_HOME="D:\App\Microsoft\jdk-25.0.4.101-hotspot"
       mvn clean package

  2) bash / Git Bash：
       JAVA_HOME=/path/to/jdk-25 mvn clean package

  3) 永久生效：把系统环境变量 JAVA_HOME 指向 JDK 25 目录，然后重开终端。

  4) 多 JDK 共存时推荐用 Maven Toolchains：
       在 ~/.m2/toolchains.xml 中声明 JDK 25，并在 pom 中启用 maven-toolchains-plugin，
       这样无论 JAVA_HOME 指向哪个版本都能正确选择 JDK 25。

确认当前实际使用的版本：mvn -version
                                    </message>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>


</project>
